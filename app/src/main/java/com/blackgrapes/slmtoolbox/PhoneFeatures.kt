package com.blackgrapes.slmtoolbox

/**
 * Product switches for the field app.
 * Estimate / kit BOQ live on desktop; phone is survey + rules sync.
 */
object PhoneFeatures {
    /** When false, Estimate UI is disabled and catalog sync is rules-only. */
    const val ESTIMATE_ENABLED: Boolean = false
}
