# Copy these into local.properties (project root) for rental builds.
# Leave empty / omit for local dev (licensing stays OFF).
# Also set sdk.dir for Android builds (Android Studio usually writes this).

# SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# SUPABASE_ANON_KEY=your_anon_public_key
